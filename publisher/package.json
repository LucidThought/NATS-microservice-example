{
  "name": "publisher",
  "version": "1.0.0",
  "description": "This folder (/publisher) holds the Node project that will send messages through the NATS messaging system to the service that is housed in the /node folder.",
  "main": "server.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon --watch './**/*.ts' --exec 'ts-node' src/server.ts"
  },
  "author": "",
  "license": "UNLICENSED",
  "dependencies": {
    "nats": "^2.13.1"
  },
  "devDependencies": {
    "@types/node": "^18.15.13",
    "gts": "^3.1.1",
    "nodemon": "^2.0.22",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.4"
  }
}
